<main class="ingredient-card">
    
    <div class="ingredient-card-body">
        {{!--
        <div class="ingredient-photo-container">
            <img class="ingredient-photo" src="{{ImageURL}}" />
        </div>
        --}}
        
        <p class="icb ingredient-producer-name"> Produced by {{producerName}}</p>
        <p class="icb ingredient-score">Ethical Rating: {{ethicScore}}</p>
        <a class="icb ingredient-rate-button rate-prod" href="#">+Rate Them</a>
        {{!--
        <div class="ingredient-score-container">
            <div class="ingredient-score">
                <a>Ethical Rating: {{ethicScore}}</a>
            </div>
            <a class="ingredient-rate-button" href="#">+Rate Them</a>
        </div>
        --}}
        {{!rate producer button}}
    </div>
</main>